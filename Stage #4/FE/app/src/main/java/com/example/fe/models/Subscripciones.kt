package com.example.fe.models

class Subscripciones (plan: Int, precio: Float, estado: Boolean) {
    val plan: Int       = plan;
    val precio: Float   = precio;
    val estado: Boolean = estado;

}